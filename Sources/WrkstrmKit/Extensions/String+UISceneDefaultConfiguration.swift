import Foundation

extension String {
  /// The default configuration name for a UIScene. Related to `UISceneConfigurationName`.
  /// Expected to match the Info.plist file for default configurations.
  public static let defaultConfiguration: Self = "Default Configuration"
}
