@Metadata {
  @TechnologyRoot
}

# WrkstrmKit

## Overview

- Goal: provide UIKit helpers and view utilities used across Wrkstrm apps.
- Scope: layout, animation, and small view extensions that keep UI code consistent and readable.

Use this catalog to explore patterns such as the chainable `UIViewAnimation` API that wraps
`UIViewPropertyAnimator`.

## Articles

- <doc:UIViewAnimationDesign>

